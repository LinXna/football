# 复审记录

## 2026-08-22 全量量化算法精修、孤立特征激活与后端拓扑治理复审

本轮针对全链路量化计算准确性、雷速数据休眠特征激活以及后端服务依赖完整性进行了深度治理：

1. **亚洲让球盘连续泊松积分升级 (`computeAsianHandicapProb`)**：
   - 彻底废除离散阶梯估值，引入全场及半场 $7 \times 7$ 独立泊松矩阵双重循环积分；
   - 自动解析半球、平半、一球等盘口线，精确计算赢半（+0.25）、输半（-0.25）与平手走盘（0），生成平滑的物理胜率与 Edge 差值。
2. **半场时间衰减平滑化**：
   - 半场进球期望采用平滑时变折算，消除 44~45 分钟分母临界点的突变跳跃。
3. **角球让球盘攻防倾角加权融合**：
   - 将当前角球份额（60% 权重）与前场危险进攻压迫倾角 Field Tilt（40% 权重）进行深度加权（`blendedHomeShare = 0.60 * cornerShare + 0.40 * dangTilt`）。
4. **雷速 5 大休眠数据维度全面激活**：
   - 彻底打通交锋历史 (`head_to_head`)、近期战绩 (`recent_matches`)、联赛积分榜 (`league_standings`)、进球时段分布 (`goal_distribution`) 与盘路走势 (`trend_summary`) 的清洗与量化流转；
   - 在 $t \ge 60'$ 推演中引入 76-90+ 分钟高进球偏好 $1.10\times$ 爆发动能加权。
5. **后端拓扑 100% 连通（0 孤立文件）**：
   - 将 `batchSupplementTypes.ts` 与 `geminiEvaluationTypes.ts` 重新接入对应服务，实现 `server/` 下全部 46 个文件 100% 激活运行。

验证结果：`npm run lint` 零错误通过，端到端 56 项全真数据审计通过，生产编译成功。

## 2026-08-22 全系统文档更新与代码库拓扑深度对齐审计

本轮针对系统升级后遗留的文档断层、模块路径偏差与包管理器命令进行了全面核验与同步更新：

1. **包管理器统一与命令校准**：
   - 彻底清理了 `docs/OPERATIONS.md` 中遗留的 `pnpm` 命令，全面统一为 `npm install`、`npm run dev`、`npm run build`、`npm run lint` 和 `npm run test:ts`。
2. **后端模块化路由与服务拓扑对齐**：
   - 更新了 `docs/ARCHITECTURE.md` 与 `docs/OPERATIONS.md`，完整记录了 `server/routes/` 9 大领域隔离路由与 `server/services/` 8 大精算与调度服务引擎的职责边界。
3. **AI 评估与多 Key 轮换规范同步**：
   - 在 `LOCAL_STARTUP.md` 与 `docs/AI_SYSTEM_AND_DATA_CONTRACT.md` 中明确了 `GEMINI_API_KEY` 与 `GEMINI_API_KEYS`（逗号分隔池）的双重配置支持，补充了 429 熔断冷却与速率门禁机制说明。
4. **统一数据契约 (Canonical Contract v3.0.0) 路径修正**：
   - 修正了 `docs/AI_SYSTEM_AND_DATA_CONTRACT.md` 中指向旧路径的引用，精准对齐 `server/services/advancedTacticalQuantitativeEngines.ts`、`server/services/canonicalMatchModel.ts` 等实际文件位置。

验证结果：`npm run lint` 零错误通过，`npm run test:ts` 37 项测试通过，生产构建通过。

## 2026-08-20 全链路量化对齐、动能引擎融合与串关风控加固复审

本轮针对“预测准确率与数学建模”、“动能时序与跨批次盘口融合”以及“串关去重与风控”进行端到端重构与复审：

1. **角球与优势指数动态方程排查**：
   - 确认角球与纪律乘数 100% 采用动态方程（包含爆发速率、边路挤压比率与泊松期望转化）。
   - 彻底清除了 `recommend_live.py` 等日志输出中遗留的“包含角球4-0级压制”静态示例字符串，全面采用真实数据插值。
2. **五大高阶量化数学引擎同步**：
   - 全面引入非线性时段发力期加权（56'-75' 核心攻坚期 1.22x，76'-90' 搏命期 1.28x）。
   - 引入红牌物理失衡乘数（1.75x~2.30x）与优势指数断崖偏置（±6.5）。
   - 引入历史交锋（H2H）与主客场特异性泊松先验分布。
   - 引入公允概率去水与 +EV 价值门禁（EV > 0 且 Value Edge ≥ +2.5%）。
3. **攻势动能与跨批次盘口合流 (`snapshotDeltaEngine.ts`)**：
   - 消除首次导入盲区，单批次导入时自动利用雷速 0~90 分钟攻势时序波形计算近 15 分钟斜率与压制均分。
   - 融合大小球掉盘与攻势起势，共振触发“🔥 黄金切入点”契机，并在切片无增量时消除视觉冲突。
4. **串关签名去重与重叠率熔断**：
   - 在 AI 串关规范化与前端推荐生成器中实现票据签名哈希去重，彻底消除多串关重复包含相同 2 场核心腿的缺陷。
   - 严格执行 2串1 之间允许重叠场次 = 0，2串1 与 3串1 重叠场次 ≤ 1 的风控熔断。

验证结果：`npm run lint` 通过，TypeScript 单元测试全部通过，Python 单元测试 28 项全部通过，生产构建打包成功。


本轮按导入、赛事匹配、候选生成、台账持久化、页面结算、运行数据清理和生产构建检查。

已修复：

1. 页面曾把所有滚球大小球都按“推荐后剩余进球”结算，与推荐程序写入的
   `full_match_total` 口径冲突。现仅 `remaining_goals` 扣除推荐时比分；未标注和
   `full_match_total` 均按完场总进球结算。
2. 台账候选写入曾丢弃 `recommendation.basis` 和 `scope`，导致正确口径保存后丢失。
   现已贯通单场候选、AI候选、正式推荐和串关腿展示结算所需的口径字段。
3. “清空全部分析数据”曾清空雷速快照的 `matches` 字段，但雷速标准结构使用
   `events`。现已按真实结构清空，避免界面显示已清空而导出仍夹带旧赛事。
4. 增加滚球全场总进球与剩余进球两种口径的回归测试，并验证默认采用全场口径。

验证结果：TypeScript 类型检查通过；TypeScript 5 项测试通过；Python 28 项测试通过；
离线导入/匹配集成测试通过；开发服务健康检查通过；生产构建通过。

可靠性边界：当前 `model_score` 是规则评分，不是经过概率校准的真实胜率。系统已将机器
结果限定为候选，正式建议仍要求 B 级以上、明确盘口与赔率、具体北京时间；滚球正式建议
还要求可信的推荐时比分。要把“预测可靠”量化为可审计指标，仍需持续积累未选择性删减的
正式样本，并按时间滚动回测 Brier score、校准曲线、ROI 及不同玩法/联赛的样本量。

### 第二阶段加固

- 比分写入统一校验为非负整数；拒绝缺失比分、负数、小数以及大于完场比分的半场比分。
- 复盘结果限制为受支持状态，并将单场复盘同步到滚球/赛前决策文件。
- 批量导入限制 5000 场，拒绝缺少主队或客队的数据，取消只匹配一支球队的模糊覆盖。
- 数据补充不再把 `PASS/C` 自动升级成 `WATCH/B`；新导入记录固定从 `RESEARCH/C` 开始。
- 修正带时区开赛时间到北京时间的转换，并正确累计“小时＋分钟”倒计时。
- 正式命中率、ROI、评级、玩法和串关统计严格排除机器候选；唯一方向不再因赔率变化重复计数。
- 未提供模型评分时保存为 0，不再虚构 72/75 分；启发式评分不再自动写入预测概率字段。

### 第三阶段：并发、API 与概率校准

- Node JSON 存储增加跨进程排他锁、10 秒超时、进程存活判断及崩溃残留锁恢复。
- 台账、别名、批量导入、比分补录和清理接口在完整“读—改—写”周期内持锁，多文件仍原子提交。
- Python 推荐、正式记录和复盘脚本接入同一个 `.json-store.lock` 协议，避免与网页 API 互相覆盖。
- 并发压力测试由 4 个 Node 进程和 1 个 Python 进程共同完成 100 次增量更新，结果无丢失。
- 新增隔离临时项目目录的真实 HTTP 集成测试，覆盖正式推荐校验、去重、补分、复盘、清理和受保护数据。
- 新增 `/api/calibration`：只读取正式、单场、已结算且具有明确概率的样本，输出 Brier Score、ECE、
  概率分箱、ROI、最近 30/90 条滚动指标，并对少于 30 条样本明确禁止下结论。
- 台账页面新增概率校准摘要；正式推荐保存明确的 `prediction_probability`，不使用规则分数代替。

本阶段验证：TypeScript/API/并发/校准测试 10 项通过；Python 28 项通过；类型检查、生产构建、
开发服务健康检查及差异格式检查通过。

### 第四阶段：全写点原子化与分层校准

- Node 读取JSON时只对“不存在”使用默认值；已存在但为空或解析失败的文件不再伪装成空数据。
- 每次成功覆盖保留上一版 `.bak`。损坏文件先复制为 `.corrupt-时间戳.json`；有效备份存在时
  自动恢复，否则抛出明确错误并阻止写入。
- `requireJsonWrites` 写入前同时验证新数据和现有文件，避免未经过读取的直接写操作覆盖损坏数据。
- Python新增 `read_json_strict` 与 `atomic_write_json`，采用同一跨语言锁，执行临时文件写入、
  `flush/fsync`、重新解析、持久备份和 `os.replace` 原子替换。
- `football_live.py` 的候选、趋势历史、自动别名，别名管理工具，推荐、记录、复盘及整合导出
  已全部移除直接 `write_text` JSON写入。
- 人工输赢继续保留，同时记录 `outcome_source=manual_user_confirmed`、记录时间和历史结果，满足审计。
- 校准新增按滚球/赛前、玩法、联赛、评级和模型版本分层；少于15条标记“样本不足”，15至29条
  标记“仅观察”，30条以上才标记“可初步比较”。台账页面展示各维度前五个分层结果。

复审日期：2026-08-15

## 已修复

1. Python 脚本迁移后，`analysis_menu.py`、`manage_team_aliases.py`、`export_combined_data.py` 曾将 `scripts/python/` 错当项目根目录。现已统一解析到项目根目录。
2. 根目录 Python 兼容入口曾在被测试导入时执行 CLI。核心可导入模块已改为“导入暴露函数、直接运行才执行 main”。
3. `vite.config.ts` 存在指向自身端口的 `/api` 代理，与 `server.ts` 的 Express + Vite middleware 模式冲突且容易误导，已移除。
4. `bun.lock` 与 pnpm 使用方式重复，已移除 Bun 锁文件。
5. Python 缓存文件曾被 Git 跟踪，现已从工作区清理并加入忽略规则。
6. PowerShell 脚本迁移后曾错误地将 `scripts/` 作为项目根目录；现已统一回退三级，确保能访问根目录的依赖、输入和输出。

## 当前风险

1. `server.ts` 仍较大，建议按台账、别名、导出、AI 评估拆为独立路由和服务模块。
2. 根目录兼容入口与 `scripts/` 内真实实现并存；兼容层应在外部快捷方式和自动任务全部更新后再删除。
3. `output/` 中仍保留已有运行数据和用户修改，未做破坏性清空。长期报告应放在 `reports/`。

## 验证结果

- `tsc --noEmit`：通过。
- `python -m unittest discover -s tests -p 'test_*.py'`：28 项通过。
- `export_combined_data.py --help`：通过。
- `manage_team_aliases.py --help`：通过。
