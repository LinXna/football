# ChatGPT 项目执行说明

本目录是 ChatGPT 项目“lx”的本地工作目录。

## 文件边界

- `sources/` 下的全部文件都是只读参考资料。
- 不得编辑、重命名、移动或删除 `sources/` 中的同步文件。
- 项目根目录及 `output/` 中的程序、配置和生成结果可以按用户要求维护。

## 开始任务前必须执行

凡涉及滚球、非滚球、赛事匹配、盘口导出、雷速导出、球队别名、推荐或赛后复盘，
主代理必须先完整读取：

`docs/AI_SYSTEM_AND_DATA_CONTRACT.md`

`CUSTOM_INSTRUCTIONS_COMPLETE.md`

前者是系统运行、数据结构、导入导出与AI接口的统一契约；后者是完整、持续更新的足球分析协议。两份文件都必须完整读取。不得把已经完成的系统重新当作待设计
方案，也不得要求用户重复提供本地最新输出中已经存在的信息。

随后按任务类型主动读取最新文件：

- 滚球：
  - `output/ybty_latest.json`
  - `output/leisu_latest.json`
  - `output/ybty_leisu_candidates.json`
  - `output/ybty_leisu_decisions.json`
  - `output/pipeline_status.json`
- 非滚球：
  - `output/ybty_prematch_latest.json`
  - `output/leisu_prematch_latest.json`
  - `output/ybty_leisu_prematch_candidates.json`
  - `output/ybty_leisu_prematch_decisions.json`
  - `output/prematch_ai_brief.json`
  - `output/prematch_pipeline_status.json`
- 推荐与复盘：
  - `output/recommendation_ledger.json`
  - 最新回测报告和已核实赛果文件

## 推荐的硬性约束

1. 程序输出的 `WATCH`、`RESEARCH` 或候选评分只是机器初筛，不是正式投注推荐。
2. 正式推荐必须由 AI 完成基本面、战意、阵容、伤停、赛事性质、盘口价值和风险核验。
3. 正式推荐必须写入推荐台账；未写入台账的机器候选不得计入正式命中率。
4. 所有正式单场及串关每一腿必须显示：
   - 开赛时间（北京时间）；
   - YBTY 原始队名；
   - 推荐玩法、盘口和赔率；
   - 滚球推荐时的分钟与比分。
5. 队名以最新 YBTY 导出的原始名称为准。雷速名称只用于交叉验证和赛事匹配。
6. YBTY 只有“X分钟后开赛”时，可以根据导出时间推算具体时间，并标明为推算时间。
7. YBTY 时间缺失时可使用已匹配的雷速时间补充，但必须标注“雷速补充”。
8. 比赛对象、时间或比分无法可靠确认时，不得升级为正式推荐。

## 滚球比分和盘口规则

1. 雷速滚球比分必须优先取比分画布或可靠接口字段。
2. 行文本、射门、角球等数字不得被误识别为比分。
3. 推荐记录必须保存 `score_source` 和 `score_verified`。
4. 比分未经校验时：
   - 不得给A级推荐；
   - 不得结算“剩余进球”或“后续时段让球”；
   - 赛后应标记为 `invalid_data`，不得算作赢或输。
5. YBTY 滚球大小球如果代表推荐后剩余进球：
   - 结算值＝完场总进球－推荐时已有总进球。
6. YBTY 滚球后续时段让球：
   - 使用推荐后双方新增进球结算；
   - 不得直接使用全场最终净胜球结算。
7. 2/2.5、2.5/3、-0.5/-1等四分之一盘口必须正确计算赢半、输半和走盘。

## 串关风控

1. 同一场比赛可以在不同串关中采用不同投注方式，但独赢、让球、大小球或BTTS等每个方向都必须独立研究并达到B级以上。
2. 不得为了玩法多样化临时改写市场、盘口或赔率，也不得把未经研究的方向包装成正式推荐。
3. B级同一方向最多进入一组正式串关；A级且评分不低于85、首发与战意明确时，同一方向最多进入两组。跨串重复使用同一比赛必须计入相关风险，概率不高时只保留一组。
4. 单张普通串关不重复放同一场；不得用相同核心结构反复生成3串、4串、5串，增加一条边缘腿不构成独立方案。
5. 同一轮杯赛、相同轮换环境或相同风险类型属于相关风险，不能按完全独立事件处理。
6. 每一腿必须独立达到B级以上并完成必要研究；不足时减少腿数或不做串关。
7. 杯赛、友谊赛和强弱悬殊比赛在正式首发、轮换及战意未确认前最高C级，不进正式串关。
8. 强队可能获胜不等于能够穿深盘；推荐深盘必须有同级净胜幅度与正式阵容支持。
9. 没有符合A级例外的核心方向时原则上只输出一组串关；至少一个合格A级方向才考虑第二组，至少两个合格A级方向才考虑第三组，且仍须通过相关性审查。

## 复盘和统计口径

1. 必须区分：
   - `machine_candidate`：程序候选；
   - `formal_ai_recommendation`：正式AI推荐。
2. 命中率、盈亏率和正式表现只统计正式推荐。
3. 重复运行产生的相同比赛、分钟、比分、市场、盘口和赔率记录必须去重。
4. 同一投注方向出现在多个串关中：
   - 方向统计只算一个唯一样本；
   - 串关结果按每一张组合分别结算；
   - 不得用重复组合放大推荐场次或命中率。
5. 串关只要一腿确定失败，整组即可判负；其他尚未核实的腿仍保留逐腿 `pending`。
6. 未取得可靠完赛结果的比赛必须保持 `pending`，不得猜测或根据不完整页面填写。
7. 回测必须同时报告：
   - 正式单场和正式串关；
   - 唯一投注方向；
   - 赢、输、走、赢半、输半；
   - 无效数据；
   - 尚未核实的记录；
   - 错误类型和下一轮实际拦截规则。
8. 复盘发现固定问题后，必须修改程序或协议形成硬性拦截，不能只写文字反省。

## 2026-07-29 回测形成的强制规则

1. 蔚山HD方向被重复放入四组串关，一次误判同时击穿全部组合；以后B级或未经独立复核的核心腿只进一组，符合A级例外时同一方向最多两组。
2. 杯赛不得仅凭联赛级别、球队名气和低独赢赔率推荐热门强队。
3. 独赢缺乏安全边际时，不得再追加该队深盘扩大同一错误。
4. 串关数量不是质量；只有一组结构合理时只输出一组。
5. 相关报告：
   - `output/BACKTEST_REPORT_2026-07-29.md`
   - `output/formal_results_2026-07-29.json`

## 执行风格

- 用户要求分析、推荐或复盘时，直接读取现有输出并完成任务。
- 不连续回复“继续收集”“正在处理”等没有实际结果的内容。
- 滚球优先速度：先快速筛选，再对少量候选深挖。
- 数据不足时明确说明缺口，但必须先穷尽现有本地数据和可用可靠来源。
- 不承诺必胜，不编造实时数据，不为满足腿数或倍数降低标准。

## 整合数据导出

- `run_analysis.cmd` 的图形界面包含滚球、非滚球两个独立整合导出按钮。
- 导出程序为 `export_combined_data.py`，保存地址必须由用户在窗口中选择。
- 导出必须使用流程状态文件记录的本次分析实际输入快照，不能把不同批次数据混合。
- 整合文件必须保留YBTY和雷速完整原始数据、成功匹配、未匹配明细、决策及流程状态。
- 非滚球整合文件还必须包含 `prematch_ai_brief.json`。
- 不得为减小文件而删除原始赛事、详情数据或未匹配记录；完整性异常必须明确提示。
